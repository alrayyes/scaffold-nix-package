# What `nix build`/`nix-build` produce, not what an editor or OS drops
# beside it — those live in ~/.config/git/gitignore_global, not here.
result
result-*

/example

node_modules/
.bun-cache/

# Written by the release job, not by hand.
CHANGELOG.md
