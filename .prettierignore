# Prettier is here to fill the gaps around Nix's own tooling, which formats
# and lints *.nix itself — today that means Markdown and YAML.
node_modules/
.bun-cache/
bun.lock
flake.lock

# Written by the release job, not by hand.
CHANGELOG.md

# `openspec init`'s generated skill/command docs — somebody else's docs,
# held to somebody else's rules.
.claude/
